import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  eslint: {
    // dirs: ['pages', 'components', 'utils'], // Pastikan direktori ini benar dan tidak kosong
  },
};

export default nextConfig;
